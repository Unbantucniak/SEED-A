# 开发迭代日志

## 2026-03-23 第1轮（已完成）

### 核心修复
- 修复混合检索综合得分的变量引用风险，确保每条结果使用自身经验对象计算分数。
- 为路由引擎补充策略使用统计接口，修复监控健康检查依赖缺口。
- 统一路由禁用策略配置为小写匹配，避免大小写导致禁用失效。
- 增强 G3 参数校验：空种子、非法种子、rounds 小于等于 0 会明确报错。

### 验证结果
- 单元测试通过：17/17。
- G3 最小场景可运行并生成报告。

## 2026-03-23 第2轮（进行中）

### 目标
- 将经验管理模块的关键 print 输出迁移为 logging，降低长实验噪音并提高可观测性。
- 修复 G3 脚本路径解析歧义，避免从项目根运行时输出目录落到 experiments/experiments/results。
- 完成本轮回归验证并补充记录。

### 已完成改动
- ExperienceManager 日志治理：将抽取失败、质量阈值拦截、入库成功、清理结果、导入导出结果统一改为 logging。
- G3 路径解析修复：
	- 默认参数仍按脚本目录解析，保持历史行为。
	- 自定义参数按当前工作目录优先解析，避免 output-dir 重复嵌套。

### 待验证项
- 单元测试回归（17/17）。
- G3 最小场景输出路径验证（自定义 output-dir 是否落到预期目录）。

### 验证结果
- 单元测试回归通过：17/17。
- G3 最小场景通过，自定义 output-dir=experiments/results 产物已正确落盘到 experiments/results。
- 说明：experiments/experiments/results 中仍保留历史误输出文件（本轮未删除，避免误清理）。

## 2026-03-23 第3轮（已完成）

### 目标
- 按“完成1+2”继续推进：
	- 1) 清理历史误输出目录并保留有效产物。
	- 2) 继续异常治理（消除裸 except）。
- 落实“每次实验记录过程与结果”的自动化机制。

### 已完成改动
- 新增统一实验记录器：`experiments/run_logger.py`。
- 已接入自动记录脚本：
	- `experiments/run_experiment.py`
	- `experiments/run_g1_experiment.py`
	- `experiments/run_g2_analysis.py`
	- `experiments/run_g3_ablation.py`
- 每次运行会在输出目录追加 `experiment_run_log.md`，记录参数与产物路径。
- 清理历史误输出：已将 `experiments/experiments/results` 的文件迁回 `experiments/results`，并删除嵌套目录。
- 异常治理：清理 `vector_embedding.py` 中裸 `except`，改为显式异常类型并记录 debug 日志。
- README 已补充“实验过程日志”机制与路径解析说明。

### 验证结果
- 单元测试回归通过：17/17。
- G3 最小场景运行通过，新增产物：
	- `experiments/results/g3_ablation_20260323_140011.json`
	- `experiments/results/g3_ablation_20260323_140011_report.md`
- 自动实验日志文件已生成并包含本次运行记录：
	- `experiments/results/experiment_run_log.md`
